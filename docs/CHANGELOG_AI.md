# QORAI — журнал изменений

## 2026-09-23 — продуктовая подача README

- По уточнению владельца изменены только README и память документационной задачи. Верх README объясняет пользу QORAI, особенности связки коррекции спроса, прогноза, календаря и правил закупки; добавлены обзор возможностей и сценарий работы.
- Описана целевая работа через CRM/ERP, обмен Excel/CSV и ручные формы без выдачи будущих адаптеров за готовые. План складов, клиентских партий, резервов и управляемого улучшения модели сохранён в нижней части.
- После обновления изолированной базы до `bf85b04` отражены реализованные PR #3 коррекции и демонстрационные условия. Нижняя матрица отличает готовое, частичное и план; ограничения данных и эксплуатации сохранены.
- Проверки: 9 относительных путей README, якорь примера и строгий UTF-8 PASS; `git diff --check` PASS. Review ведущего завершён. Runtime/тесты не повторялись; код и данные не менялись. Публикация документации — отдельным draft PR без merge/deploy.

## 2026-09-23 — операции, остатки и оценочная коррекция спроса

- Потоковый импорт положительных расходных документов: точный код/единица, один документ вместо отдельных наблюдений строк, исключение неподходящих/будущих строк, без раскрытия идентификаторов. Добавлены начальные месячные остатки; raw sales и SHA12 неизменны. Теперь 10 parsed / 2 seasonality hash-only.
- Demand-ядро выделяет документный избыток медианой/MAD и оценивает возможные потери при нулевом начальном остатке/низких продажах по прошлым опорам. Сезонная защита, нерекурсивная компенсация, исходные цели MAE; demo-policy сохраняется в результате/CSV.
- Data QA: 15 passed/0 skips/40.98s; load 30.56s. Core focused 122 passed, дополнительная адресная валидация 48 passed. Независимая release/browser-проверка ещё фиксируется отдельно; Git/deploy этой итерации не заявлены выполненными.
- README, ALGORITHM, CASE_COMPLIANCE, API_CONTRACT и память синхронизированы с реализованными оценками, без обещаний client-level identification, точных дней stockout и полного закрытия кейса.
- По срочному разрешению владельца выпущен code-only checkpoint `a4c94fe`: 14 разрешённых файлов commit/push, remote=head. README/ALGORITHM уже отражают текущий ATN540126 0→24/дефицит03.10 вместо прежних36/01.10. Новый настоящий Fable завершился exit0/182.746s; документы и полный release ещё проходят приёмку.
- Независимый итоговый Python-прогон текущей версии: **161 passed / 0 skips / 233.19s / exit0**. Browser/demo и Git-приёмка документов выполняются отдельно; результаты прежних версий оставлены историческими.
- Ведущий принял **73 frontend checks** и новый browser demo/Q24/corrections/CSV UI; errors[]. Выпуск8docs/PR/merge/Render sync разрешён после этой приёмки; этот checkpoint не выдаётся за уже выполненный deploy.

## История: 2026-09-23 — статус каталога перед PR #2 и аудит соответствия

- По подтверждению ведущего основной MVP merged: PR #1, `main` SHA `53aae44`; [Render LIVE](https://qorai.onrender.com) пока с прежним UI. Новый каталог готов к PR после algorithm-first README; новый release ещё не объявлен выполненным.
- Локальная integrated QA: **127 passed / 0 skips / 28.83s / exit 0**. После финальных microcopy/scroll изменений Node syntax check PASS, **18 helper assertions + 23 catalog checks PASS**, exit 0.
- Browser QA ведущего: первый экран без autoselect, каталог3045 (IEK2463/SystemElectric582), источник ATN540126 42/19/23/pack6 напрямую сверен с XLSX; технический сценарий ETA08.10 дал Q36 → черновик → передача CSV браузеру. Новое сохранение на диск не подтверждено. Пагинация, пустое состояние, null и errors `[]` зафиксированы в QA/E2E, evidence04–08 остаются ignored.
- Реальный Claude Fable-аудит оригинального кейса и независимая проверка схемы сохранены в CASE_COMPLIANCE: must-have1/2/5 partial,3/4 не реализованы; 8 parsed +4 hash-only. Операции есть, но clientID/дневная доступность в проверенных таблицах не найдены. Это явное ограничение продукта, не закрытое пустыми значениями требование.
- Актуализированы верхние разделы QA_REPORT/E2E_BOT_REGISTRY/PROJECT_STATE/AI_HANDOFF и журнала; прежние результаты оставлены как история. Код/README этим worker не менялись, Git/deploy не выполнялись.

## История предыдущих этапов

## 2026-09-23 — итоговый локальный release-check

- Безопасное получение публичных исходных архивов: scripts/bootstrap_data.py и18 тестов, pinned SHA256 архивов/12 XLSX, запрет небезопасной распаковки/перезаписи; исходники не stage.
- `.python-version=3.13`; README/RUNBOOK/DEPLOYMENT содержат автоматический bootstrap и переносимый запуск. Хостинг остаётся у владельца, URL не создавался.
- После новых API/helper изменений выполнен один общий `py -m pytest -q`:127passed/0skips19.43s. Свой preview проверен и обновлён; новыйPID7492,health/bootstrapPASS. Ведущий повторно принял QORAI в браузере, errorlogs=[]; функциональных блокеров не осталось.
- ROADMAP и TECH_SPEC очищены от устаревших guessed API/CLI-only статусов; текущая память консолидирована. Mobile NOT VERIFIED и Docker build NOT RUN сохранены честно. Git-публикация ещё ожидает финального GO.

## 2026-09-23 — локальный API и README для жюри

- По решению владельца официальное имя — QORAI — управление запасами; README и актуальная документация обновлены. Технические имена пакета/репозитория сохранены.

- Добавлен FastAPI поверх существующего ядра: bootstrap/item/plan, серверный CSV, прогнозный CSV, безопасный reload, health и static web. Один localhost-процесс, без внешних заказов/БД/LLM.
- API19PASS/0skips9.58s, livehealthPASS. Защищены null/blocked, типы/даты/параметры, формулы CSV, duplicate-SKU сценарии, stale result_id, источники/остаток, локальный origin и сохранение снимка при ошибке reload.
- Независимый reviewer подтвердил91passed/0skips20.30s и инварианты всех3045 SKU при явно технической политике; P0/P1 app/replenishment не выявлены. Веб-приёмка остаётся отдельным незакрытым этапом.
- Browser evidence добавлен: ATN540126/ETA задержки Q0→36, неизменные исходники; скачанный CSV прочитан и совпал с расчётом. Ошибка stale draft исправлена UI-worker и повторно проверена; поиск без совпадений/очистка с клавиатуры PASS. Reload/design QA остаются открытыми.
- Следующая browser-проверка закрыла reload: сохранены SKU/дата источника, старый расчёт/draft сброшены, экспорт отключён; объяснение Q36 из raw35.19/pack6 проверено. Итоговая визуальная/mobile приёмка ещё ожидается.
- Отдельный исполнитель подготовил Python/Docker-hosting и явный Host/Origin allowlist; API28PASS/9.95s, локальный smokePASS. README/RUNBOOK/API/docs синхронизированы; Docker build не выполнен, публичного URL/deploy нет.
- Добавлен design-qa.md по3 реальным desktop-снимкам ведущего; мобильный viewport фактически не изменился, поэтому mobile NOT VERIFIED. Визуальные намеренные отличия и остаточный риск записаны без фиктивного процента совпадения; снимки/референсы не включаются в Git.
- После проверки ведущим добавлен точный PUBLIC_ORIGIN для TLS-proxy без wildcard; последняя API-регрессия37PASS/0skips13.32s. Документация отличает эту проверку от прошлого полного91PASS и от ещё не выполненного публичного deploy.
- README описывает пользу, запуск, 6 ручных проверок и реальные ограничения. Созданы API_CONTRACT, RUNBOOK, QA_REPORT, E2E_BOT_REGISTRY; browser QA/общий независимый прогон отмечены как ожидаемые, не PASS.
- Версии уже установленных FastAPI/uvicorn/Pydantic/httpx закреплены в requirements. Fetch/prune успешен, main актуален; новый commit/push/PR/merge/deploy этим этапом не выполнялся.

## 2026-09-23 — интеграция CLI и фактическая проверка расчёта

- Добавлены `smartbuyer/cli.py`, `tests/test_cli.py`, requirements и scoped .gitignore. JSON/CSV из одного результата, только stdout, происхождение/дата/единицы, отдельные calculation/forecast/order статусы, безопасный CSV-текст.
- README получил команды запуска и ограничения. Общая проверка data+forecast+CLI: 44 passed без skips; CLI отдельно 13 passed, compile PASS.
- Выполнен прогон всех позиций только по выданным XLSX, без новых бизнес-fixtures и смешанного общего MAE; все точные закупки blocked. Выбранный пример показал худший MAE сезонного кандидата, а не доказанное улучшение.
- Источники, чужие worker-файлы и design-материалы не изменялись; stage/commit/push/merge/deploy не выполнялись.
- По независимому QA сохранён stock_status в CSV с тестом равенства JSON; объяснения стали русскими без None/длинных дробей, числовой результат не округляется. Посчитаны только SKU-counts побед по MAE, без общей ошибки разных единиц и без автоматического выбора модели.

## 2026-09-23 — настоящий Fable и узкий расчётный срез

- Claude Fable реально запущен через launcher и ответил read-only; по ответу созданы [TECH_SPEC.md](TECH_SPEC.md) и [ROADMAP.md](ROADMAP.md), 135 строк суммарно. Условная техническая поддержка ядра не объявляет весь кейс решённым.
- Последнее решение о данных преобладает: только выданные 12 XLSX, без своего DEMO-001 и новых бизнес-fixtures; unknown не0, история источников и отсутствие входов видимы.
- По приоритету владельца ведущий запустил data/forecast/tests в независимых write-set; интегратор единолично владеет общими файлами. Документационный исполнитель не писал продуктовый код и не выполнял Git-публикацию/deploy.

## 2026-09-23 — сохранение плана HackAlem Logistics

- В локальном конкурсном репозитории создан единый [LOGISTICS_PLAN.md](LOGISTICS_PLAN.md); README получил ссылки, исходные строки сохранены.
- Собраны официальные требования, решения владельца, рабочие предложения, ограничения данных, критерии приёмки и датированная память обсуждения.
- Добавлена последняя логика: принятый остаток меняется по подтверждённым документам и выдаче; заказ/бронь/путь/задержка остаются отдельными состояниями.
- Зафиксированы частичная приёмка, защита от повторного импорта и двойного списания, свежесть данных и разделение факта/прогноза.
- Созданы файлы состояния и handoff. Продуктовый код, ключи, сырые продажи, реальные заказы и внешние интеграции не добавлялись.
- `fetch origin --prune` успешен; рабочая ветка `docs/logistics-plan-20260923` от актуальной `main`. Commit/push не выполнены.
- Проверки: `git diff --check`, UTF-8 и локальные Markdown-ссылки PASS; ведущий прочитал и принял документ с точечными уточнениями. Результаты в [AI_HANDOFF.md](AI_HANDOFF.md); прикладные тесты ещё не существуют и не объявляются пройденными.

## 2026-09-23 — текстовый пакет для Stitch

- Созданы дизайн-бриф и короткий prompt; четыре рабочих экрана, конкретные действия/состояния и синтетический сценарий переноса поставки.
- В план добавлены идея ночного анализа, контроль качества на временных срезах и административное применение/откат; учтено предупреждение Fable об обучении на заниженных дефицитом продажах.
- Формат desktop-first веб-приложения записан как предложение команды; SaaS и полный ML не входят автоматически в MVP.
- Код, изображения, запуск Stitch, реальные интеграции и расписание не создавались; commit/push не выполнялись.

### Уточнения перед передачей в Stitch

- По решению владельца основное размещение — его песочница; точный адрес и изоляция пока не проверены, размещения ещё нет.
- После внешней критики рекомендуемый дизайн сокращён до трёх представлений основного пути и одного сценария задержки; складские операции, бронирование и обучение оставлены за границами первого объёма.
- Приоритет крупному клиенту сохранён; изменения приоритетов не утверждаются за владельца. Расхождение источников и отсутствие финальных формул явно видны.
- Получена новая авторизация владельца на commit/push документов; ведущий выполнил финальную проверку и разрешил публикацию рабочей ветки без PR, merge и deploy.
- Сохранён предлагаемый расчётный контракт v0.1 после Fable/логической проверки: без двойного спроса и исчезновения просрочки, с явными неизвестными, формулой заказа и честным сравнением с базовым методом. Это не реализованный/валидированный алгоритм.
- Итоговые проверки текста, ссылок, кодировки, синтетической арифметики и отсутствия шаблонов секретов PASS; две копии Stitch на рабочем столе совпадают по SHA256. Документация принята ведущим.

## 2026-09-23 — план исполнения до дедлайна

- Добавлен [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md): последовательность со слайда, бюджет 145 минут до фиксированного дедлайна, узкий MVP, входные контракты, два потока, золотые тесты и безопасная сдача. Это план, не разрешение начать код/развёртывание.
- По сообщению владельца закрыт вопрос выбора трека: он самостоятельно переключил на логистику, появилась кнопка «Сдать решение». Сдача через неё не выполнялась.
- После критики Fable записаны кандидатные правила документных аномалий и компенсации подтверждённого отсутствия с честными ограничениями, а в план исполнения — арифметические эталоны; код и калибровка не выполнены.
- Ведущий принял финальную документацию; проверки кодировки, ссылок, whitespace и секретшаблонов PASS, разрешены commit/push шести документов в рабочую ветку. Реализация продукта не начата.
