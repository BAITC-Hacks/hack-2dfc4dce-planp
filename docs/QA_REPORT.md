# QORAI — проверка MVP, 2026-09-23

## Срез приёмки перед PR, 23.09.2026

- На момент этого checkpoint основной MVP опубликован через PR #1, SHA `53aae44`, [Render LIVE](https://qorai.onrender.com). Новый каталог, README и ALGORITHM приняты ведущим; итог следующего release проверяется отдельно по GitHub/Render. Этот отчёт фиксирует доказательства локальной приёмки, не заранее объявленную публикацию.
- Последний полный прогон: **127 passed in 28.83s, 0 skips, exit 0**. После последних правок текста риска и scroll-to-top повторены `node --check web/app.js` (exit 0), `node web/app.test.cjs` (**18 assertions PASS**, exit 0) и `node tests/frontend_catalog.cjs` (**23 checks PASS**, exit 0) на реальном локальном API. Последний разрешённый `git diff --check` завершился exit 0 до этих финальных изменений; новый Git-прогон ожидает GO.
- Свежая browser QA ведущего: первый экран без автоматического выбора товара; каталог **3045 = IEK 2463 + SystemElectric 582**. ATN540126 показывает **42 / 19 / 23, упаковка 6**, что ведущий непосредственно сверил с исходным XLSX (`AX258/AY258/AZ258`). Неизвестный остаток IEK показан как «Нет данных».
- Путь ATN540126 → условия с явными техническими L2/H30/SS7/MOQ0 и ETA 08.10.2026 → **Q36** → черновик → CSV: PASS. Сервер сформировал CSV и передал браузеру; сохранение нового файла на диск и его обратное чтение отдельно **не подтверждены**. Это не повтор старой проверки скачанного Q300-файла ниже.
- Пагинация: следующая страница **2 из 99**; поиск без совпадений — **1 из 1, 0–0**. Browser errors: `[]`. После reload ведущий проверил в браузере окончательный h1 «Без нового заказа — риск дефицита с 01.10.2026», **Q36** и `window.scrollY=0` после явной навигации/расчёта: PASS.
- Локальные evidence: `outputs/audit-04-new-catalog.png`, `audit-05-source-row.png`, `audit-06-new-conditions.png`, `audit-07-calculated-overview.png`, `audit-08-order-draft.png`, `audit-09-final-calculation.png`. Ведущий визуально просмотрел все снимки 04–09: PASS. Снимки остаются ignored, не частью Git.
- Область PASS — технический desktop MVP и перечисленные пути, **не весь конкурсный кейс**: must-have 1/2/5 частичны, 3/4 не реализованы. В источниках **8 parsed + 4 hash-only**; операции есть, но clientID и ежедневная доступность не найдены в проверенной схеме. Основания — [CASE_COMPLIANCE](CASE_COMPLIANCE.md). Mobile NOT VERIFIED; Docker build NOT RUN.

## История проверок до нового каталога

Следующие записи сохраняют evidence и ограничения на момент прежних этапов. Их утверждения об отсутствии URL/deploy не описывают актуальное состояние выше.

## Подтверждённые проверки

- **Последний интегрированный прогон после готовности каталога:** `py -m pytest -q` → **127 passed in 28.83s, 0 skips, exit 0**. Использован сохранённый исходный набор `04_Excel` через `HACKALEM_DATA_DIR` и `HACKALEM_DATA_ROOT`; новые бизнес-данные не создавались. Предыдущий общий результат —127 passed in19.43s.
- На том же срезе через bundled Node: `node --check web/app.js` — exit0; `node web/app.test.cjs` — **18 frontend helper assertions PASS, exit0**; `node tests/frontend_catalog.cjs` — **23 catalog checks PASS, exit0** на фактическом API `127.0.0.1:8765`. Это проверки кода и данных каталога, не замена браузерного E2E.
- `git diff --check` — **exit0**; только предупреждения о возможной замене LF на CRLF, ошибок пробелов не выявлено. Commit/push этим исполнителем не выполнялись.
- Bootstrap-helper:18 адресных тестов, фактическая загрузка двух публичных архивов и совпадение SHA256 всех12 XLSX с исходным набором подтверждены исполнителем/ведущим; загруженный набор читает3045 позиций. Новые бизнес-данные не создавались.
- Preview после тестов обновлён: собственный старый процесс проверен/остановлен, новый PID7492 на127.0.0.1:8765. Ведущий повторно открыл браузер: QORAI,22.09.2026,3045 товаров; browser error logs=[] после reload. Health/bootstrap PASS. Функциональных блокеров релиза ведущий не выявил.

- Предыдущий импорт/прогноз/CLI: 44 passed, без skips; независимый прогон ведущего подтверждён ранее.
- `py -m py_compile smartbuyer/app.py` — PASS.
- `py -m pytest tests/test_api.py -q` — **19 passed in 9.58s**, без skips, на выданных 12 XLSX.
- `httpx==0.28.1` закреплён в requirements; повторно проверены импорт FastAPI TestClient и health-запрос HTTP200. Чистая установка зависимостей в отдельной среде не заявляется выполненной.
- Live `GET /api/health` — HTTP200, status=ok; TCP8765 слушает только127.0.0.1. Первый bootstrap TestClient читает3045 позиций и12 источников.
- Независимый reviewer: `py -m pytest tests/test_data.py tests/test_forecast.py tests/test_cli.py tests/test_replenishment.py tests/test_api.py -q` → **91 passed in 20.30s, 0 skips**. P0/P1 в app.py/replenishment.py не обнаружены; reviewer файлы не менял.
- Позднее тот же назначенный исполнитель подготовил hosting-boundary в отдельном scope: `py -m pytest tests/test_api.py -q` → **28 passed in 9.95s**,19 прежних+9 новых тестов. Exact Host allowlist, same-origin со схемой, безопасный default bind и portable launcher проверены. Локальный smoke static/QORAI/import PASS. Предыдущие91 не выдаются за повторный полный прогон после этой правки.
- Исторический прогон UI-исполнителя после ребрендинга: `node --check web/app.js` exit0; `node web/app.test.cjs` → **18 frontend helper assertions PASS**. Последующие изменения каталога проверены новым прогоном выше; helper checks не подменяют browser QA и не создают бизнес-fixtures.
- Последняя узкая API-регрессия после root review: `py -m pytest tests/test_api.py -q` → **37 passed in 13.32s,0 skips**. Проверен явный HACKALEM_PUBLIC_ORIGIN для TLS-proxy/HTTP-backend, согласование с allowlist и отказ чужим Host/Origin; localhost-default сохранён. Это последний API-результат, заменяющий промежуточные28, без заявления нового полного прогона.
- Независимые инварианты всех3045 исходных SKU при явно технических настройках L=2,H=180,SS=7,MOQ=0,auto:2881blocked,164provisional,157 положительных. Проверены неизменность источника, blocked quantity=null, ровноH дат покрытия, достижение safety, спрос после среза и отсутствие исправления прошлых дефицитов. Это настройки теста, не политика компании и не реальные заказы.
- API: пустая политика blocked/null; одинаковый расчёт даёт тот же result_id; серверный CSV сохраняет количество/статус/источник; числовой заказ от клиента запрещён; разные сценарии одного SKU вместе отклоняются.
- Проверены отрицательные/неверные параметры, неправильная дата/ключ, формульные текстовые CSV-префиксы, отказ внешнему Origin/Host, запрет произвольного data_dir, поздняя поставка не исправляет ранний дефицит.
- Ошибка reload сохраняет прежний исправный снимок и результаты; успешный reload инвалидирует сохранённые результаты. Ошибка данных не раскрывает путь или traceback.
- Независимая проверка `/api/bootstrap.sources`: **8 read_cached_values +4 not_parsed**; обе таблицы операций и обе таблицы сезонности пока только зарегистрированы/проверены по хешу. В каталоге таблица источников явно подписывает `not_parsed` как не участвующий в расчёте. Подробная проверка схемы двух файлов операций и двух месячных остатков — [CASE_COMPLIANCE](CASE_COMPLIANCE.md).

## Проверенные браузерные пути

Browser evidence ведущего: поиск ATN544045, явные технические L2/H180/SS7/MOQ0/auto и два согласия дали условный Q300,6 сопоставимых точек, MAE67.53 против40.00; выбран mean_12. Добавление и повторное обновление черновика не создали дубль. Скачанный браузером `smartbuyer-order-draft.csv` прочитан обратно:3219байт,quantity300.0,statusprovisional,stockreported_not_confirmed,arrival2026-09-24,methodmean_12. Файл не добавляется в Git.
Первоначально найден устаревший черновик после изменения политики. UI исправлен, повторная browser-проверка PASS: после добавления Q36 изменение ETA обратно на24.09 очищает черновик, отключает экспорт и требует нового расчёта; Q0 добавить нельзя. Timeout ожидания download-события инструмента не подменяет факт реально скачанного и прочитанного файла.

Сценарий только по существующей партии ATN540126: отчётные AX42/AY19/AZ23, входящая партия120. Явные технические L2/H30/SS7/MOQ0/auto и два согласия; изменение сценарной ETA24.09→08.10 меняет Q0→36 и показывает первый дефицит01.10. Исходные сведения и количество партии не меняются. Числа совпали с независимым расчётом ведущего; это не новые бизнес-fixtures и не установленная политика компании.

Фильтр IEK плюс поиск ATN дал понятный пустой список; очистка поиска с клавиатуры вернула2463 позиции IEK. Выдуманный товар или старый результат вместо пустого состояния не показан.

Browser reload PASS: при ATN540126 с явно технической политикой и ETA08.10 получен Q36, добавлен один черновик, экспорт включён. После «Перечитать Excel» сначала показаны загрузка/недоступность действий, затем сохранены выбранный SKU и дата источника22.09.2026; рекомендации и черновик сброшены, CSV недоступен до нового расчёта.
Объяснение Q36 проверено в браузере: на07.10 расчётный баланс−16.67, страховой уровень18.51, неокруглённая потребность35.19, кратность6. Подписано именно «расчётный баланс» с оговоркой об отрицательной проекции, не отрицательном фактическом складе.

## Открытые проверки / ограничения

- Полный новый pytest и независимый критический аудит: PASS в указанном ограниченном scope, не приёмка внешнего deploy или полного кейса.
- Основные browser-пути поиска/сценария/черновика/реального CSV/reload и desktop-снимки QORAI проверены ведущим. [Design QA](design-qa.md): узнаваемые структура/палитра/две колонки, видимых desktop-наложений нет; не pixel-perfect clone.
- Mobile NOT VERIFIED: попытка390×844 не изменила фактическую ширину браузера1218. Ведущий считает это остаточным риском, не блокером desktop-first MVP; проверку телефона/узкого viewport нельзя считать выполненной.
- Подготовка Python-хостинга и public-origin/TLS guard проверены локальными регрессионными тестами. Реальный внешний TLS/домен и процесс на хостинге не проверены; публичного URL пока нет.
- Реальная внешняя песочница, HTTPS/auth и публичный выпуск не проверены и не выполнялись.
- Dockerfile/.dockerignore/serve/инструкция подготовлены; Docker CLI отсутствует, поэтому build NOT RUN. Публичного URL нет. Host allowlist не является авторизацией и не гарантирует приватность опубликованных данных.
- Проверяется арифметика условного сценария, не истинность неизвестных бизнес-параметров. Нет доказанного улучшения спроса, stockout-компенсации или полного покрытия кейса.
- Отдельный фактический Claude Fable audit по оригинальному DOCX: требования3/4 не реализованы,1/2/5 частичны. Его текстовый ответ не является browser QA; квитанция launcher содержит несогласованность `status=failed/exit_code=null` при полученном ответе и внешнем exit0. Полная матрица и уточнение «операции есть, clientID и ежедневной доступности в проверенной схеме нет» — [CASE_COMPLIANCE](CASE_COMPLIANCE.md). Приёмка технического MVP не означает приёмку всех пяти must-have.

Сценарии и границы evidence: [E2E_BOT_REGISTRY](E2E_BOT_REGISTRY.md).
